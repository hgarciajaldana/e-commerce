# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# App environment
NEXT_PUBLIC_APP_ENV=development

# Base domain for subdomain resolution in production
# e.g., if tenant is acme.mitienda.com, set this to mitienda.com
NEXT_PUBLIC_BASE_DOMAIN=localhost
